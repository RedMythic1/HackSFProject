{
  "name": "tech-interests-analyzer",
  "version": "1.0.0",
  "description": "A tool to analyze tech interests and provide relevant content",
  "main": "index.js",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "start": "cd tw && ./start_all.sh",
    "build": "cd tw && npm run build",
    "fly:build": "cd tw && npm install && npm run build",
    "fly:deploy": "flyctl deploy",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:embeddings": "ts-node src/utils/testEmbeddings.ts"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "axios": "^1.4.0",
    "jsdom": "^22.1.0",
    "glob": "^10.3.3",
    "serve": "^14.2.1"
  }
}
